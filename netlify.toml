[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Environment variables for private registries (if needed):
  # FONTAWESOME_NPM_AUTH_TOKEN - for Font Awesome Pro packages (if using Font Awesome Pro)
  # Note: No longer using @phenom/react-ds - using custom design system components

[[plugins]]
  package = "@netlify/plugin-nextjs"

