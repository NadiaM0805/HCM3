"use client";

import Home from "@/app/page";

export default function AgenticDashboard() {
  // The orchestrator is now integrated in each persona page
  // No need for a global orchestrator here
  return <Home />;
}
