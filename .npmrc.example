legacy-peer-deps=true
# Use public npm registry as default
registry=https://registry.npmjs.org/

# Note: No private registries needed - all packages are from public npm
# All design system components are custom-built, not from @phenom packages

loglevel=verbose
